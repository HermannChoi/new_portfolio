"use client";
/** @jsxImportSource @emotion/react */

import { footerStyles } from "../styles/footerStyles/footerStyles";

const Footer = () => {
  return (
    <footer css={footerStyles.layout}>
      <p css={footerStyles.text}>Thank you for reading</p>
    </footer>
  );
};

export default Footer;
